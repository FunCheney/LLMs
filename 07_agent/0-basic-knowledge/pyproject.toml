[project]
name = "0-basic-knowledge"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "XuLin", email = "xulin@micous.com" }
]
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.9.0",
    "black>=23.0.0",
    "mcp[cli]>=2.0.0",
    "openai>=1.12.0",
    "pylint>=3.0.0",
    "pytest>=7.4.0",
    "python-dotenv>=1.0.0",
    "requests>=2.31.0",
    "rich>=13.7.0",
    "tenacity>=8.2.3",
]

[project.scripts]
0-basic-knowledge = "0_basic_knowledge:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
